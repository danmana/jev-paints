export default function HowItWorksPage() {
  return (
    <main className="page narrow">
      <h1 className="title">How it works</h1>
      <p className="muted">Coming once the painting loop has been tuned. In short: Jev never sees a pixel. It gets a text description of the canvas and picks a gesture, a place, a medium and a colour from fixed lists; code turns that into brush strokes.</p>
    </main>
  );
}
